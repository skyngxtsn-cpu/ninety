name: Fetch Lineups (W杯 自動取得)

on:
  schedule:
    # 5 分ごとに実行（W杯 期間中は試合直前にスタメンが乗るのを拾う）
    - cron: "*/5 * * * *"
  workflow_dispatch: # 手動実行用

concurrency:
  group: fetch-lineups
  cancel-in-progress: false

jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Fetch lineups from football-data.org
        env:
          FOOTBALL_DATA_API_KEY: ${{ secrets.FOOTBALL_DATA_API_KEY }}
        run: node scripts/fetch-lineups.mjs

      - name: Commit and push if changed
        run: |
          git config user.name "ninety-bot"
          git config user.email "bot@users.noreply.github.com"
          if ! git diff --quiet src/lib/data/lineup-overrides-auto.json; then
            git add src/lib/data/lineup-overrides-auto.json
            git commit -m "auto: lineup update $(date -u +'%Y-%m-%d %H:%M UTC')"
            git push
          else
            echo "No changes"
          fi
